import { Layout } from 'antd';

const { Footer } = Layout;

const FooterComponent = () => {

    return (
        <Footer style={{textAlign: 'center', paddingTop: '0'}}>Developers: xxx</Footer>
    ) 
}

export default FooterComponent